name: Publish NuGet packages

on:
  workflow_dispatch:
    inputs:
      package-version:
        description: "Package version to publish"
        required: false
        type: string

jobs:
  publish:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    env:
      CONFIGURATION: Release
      PACKAGE_PROJECT: src/Nvim.Client/Nvim.Client.csproj
      PACKAGE_OUTPUT: artifacts/packages
      GITHUB_PACKAGE_SOURCE: https://nuget.pkg.github.com/neovim/index.json
      NUGET_ORG_SOURCE: https://api.nuget.org/v3/index.json
      PACKAGE_VERSION: ${{ inputs.package-version }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore

      - name: Build
        run: >
          dotnet build
          --configuration "$CONFIGURATION"
          --no-restore
          -p:ContinuousIntegrationBuild=true

      - name: Pack
        run: |
          mkdir -p "$PACKAGE_OUTPUT"

          VERSION_ARG=""
          if [ -n "$PACKAGE_VERSION" ]; then
            VERSION_ARG="-p:PackageVersion=$PACKAGE_VERSION"
          fi

          dotnet pack "$PACKAGE_PROJECT" \
            --configuration "$CONFIGURATION" \
            --no-build \
            --output "$PACKAGE_OUTPUT" \
            -p:ContinuousIntegrationBuild=true \
            -p:IncludeSymbols=true \
            -p:SymbolPackageFormat=snupkg \
            $VERSION_ARG

      - name: List package artifacts
        run: ls -la "$PACKAGE_OUTPUT"

      - name: Upload package artifacts
        uses: actions/upload-artifact@v7
        with:
          name: nuget-packages
          path: |
            artifacts/packages/*.nupkg
            artifacts/packages/*.snupkg

      - name: Publish to GitHub Packages
        run: |
          for package in "$PACKAGE_OUTPUT"/*.nupkg; do
            dotnet nuget push "$package" \
              --source "$GITHUB_PACKAGE_SOURCE" \
              --api-key "${{ secrets.GITHUB_TOKEN }}" \
              --skip-duplicate
          done

      - name: Publish packages to nuget.org
        run: |
          for package in "$PACKAGE_OUTPUT"/*.nupkg; do
            dotnet nuget push "$package" \
              --source "$NUGET_ORG_SOURCE" \
              --api-key "$NUGET_API_KEY" \
              --skip-duplicate
          done
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
