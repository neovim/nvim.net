# NvimClient

This project contains common `nvim` and `MsgPack` types and provide implementations
that is reusable both in terms of API Generation logic as well as the actual plugin
implementation. This project provides the following namespaces:

1. `NvimClient`
2. `NvimClient.Models.Nvim`
3. `NvimClient.Models.MsgPack`