# NvimClient

This project contains common nvim types and how those relate to nvim communication
in terms of msgpack encoding. Those are contained in the `NvimMsgpack` directory
and `NvimMsgpack/Models` 